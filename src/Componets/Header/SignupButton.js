import { Box, Button } from "@material-ui/core";
const SingupButton = (props) => {
  return (
    <>
      <Box>
        <Button {...props}>Sing In</Button>
      </Box>
    </>
  );
};
export default SingupButton;
